import { SxProps, Theme } from "@mui/material";
import { design } from "@/themes/design";

export const styles = {
    root: {
        backgroundColor: design.sliderColor,
    } as SxProps<Theme>,
};

import options from '../schemas/options'
import recipe from '../schemas/recipe'
import favorites from '../schemas/favorites'
import recipeNotes from '../schemas/recipeNotes'
import recipeComment from '../schemas/recipeComments'

export const schemaTypes = [recipe, options, favorites, recipeNotes, recipeComment]
